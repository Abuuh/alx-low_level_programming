#include <stdio.h>

/**
 * _isupper -checks if a chhrarcter is uppercase
 * @c: the character to check
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
if (c >= "A" && c <= "Z")
Return(1);
else
Return(0);
}
